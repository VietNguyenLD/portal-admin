export interface Distributor {
  name: string;
  // id: number;
  // created_at: string;
  // updated_at: string;
  // type: number;
  // identity: string;
  // status: number;
  // ref_email: string;
  // ref_address: string;
  // ref_phone_number: string | null;
  // ref_contact_person: string | null;
  // notes: string | null
}
